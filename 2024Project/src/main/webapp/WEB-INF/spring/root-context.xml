<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
           http://mybatis.org/schema/mybatis-spring https://mybatis.org/schema/mybatis-spring-1.2.xsd">

    <!-- ✅ Spring 환경 변수 사용 가능하도록 설정 -->
    <context:property-placeholder location="classpath:database.properties"/>

    <!-- ✅ HikariCP 설정 (MariaDB 사용) -->
    <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
        <property name="driverClassName" value="org.mariadb.jdbc.Driver"/>
        <property name="jdbcUrl" value="jdbc:mariadb://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?serverTimezone=UTC&amp;characterEncoding=UTF-8"/>
        <property name="username" value="${MYSQLUSER}"/>
        <property name="password" value="${MYSQLPASSWORD}"/>
    </bean>

    <!-- ✅ HikariCP DataSource 빈 설정 -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <constructor-arg ref="hikariConfig"/>
    </bean>

    <!-- ✅ MyBatis SqlSessionFactory 설정 -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- ✅ MyBatis Mapper Scan 설정 -->
    <mybatis-spring:scan base-package="cs.dit.mapper"/>

    <!-- ✅ Spring Component Scan 설정 -->
    <context:component-scan base-package="cs.dit.domain"/>
    <context:component-scan base-package="cs.dit.service"/>
    <context:component-scan base-package="cs.dit.controller"/>

</beans>
